<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liu.community.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="com.liu.community.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue Mar 10 12:26:35 CST 2020.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="gmtcreate" jdbcType="BIGINT" property="gmtcreate" />
    <result column="gmtmodified" jdbcType="BIGINT" property="gmtmodified" />
    <result column="creator" jdbcType="INTEGER" property="creator" />
    <result column="comment_cont" jdbcType="INTEGER" property="commentCont" />
    <result column="viewcont" jdbcType="INTEGER" property="viewcont" />
    <result column="likecount" jdbcType="INTEGER" property="likecount" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
  </resultMap>
  <update id="incView" parameterType="com.liu.community.model.Question">
    update question
    set
    viewcont = viewcont + #{viewcont,jdbcType=INTEGER}
    where id = #{id}
    </update>

  <update id="incComment" parameterType="com.liu.community.model.Question">
    update question
    set
    comment_cont = comment_cont + #{commentCont,jdbcType=INTEGER}
    where id = #{id}
    </update>


  <select id="selectRelated" parameterType="com.liu.community.model.Question" resultMap="BaseResultMap">
    select * from question
    <where>
      <if test="id != null">
        and id != #{id}
      </if>
      <if test="tag != null and tag != ''">
        and tag regexp #{tag}
      </if>
    </where>
    order by gmtcreate desc limit 20
  </select>
  <select id="countBsySearch" parameterType="com.liu.community.dto.QuestionQuerDTO"  resultType="java.lang.Integer">
    select count(*) from question
    <where>
      <if test="search!=null and search != ''">
        and title  regexp #{search}
      </if>
    </where>
  </select>

  <select id="selectBySearch" parameterType="com.liu.community.dto.QuestionQuerDTO"  resultMap="BaseResultMap">
    select * from question
    <where>
      <if test="search!=null and search != ''">
        and title  regexp #{search}
      </if>
    </where>
    order by gmtcreate desc
    limit #{page},#{size}
  </select>
</mapper>